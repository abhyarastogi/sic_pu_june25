def bubble_sort(array, len_of_array):
    for i in range(len_of_array - 1) :
        for j in range()

array = list(map(int, input('Enter the elements of the array :').split()))
array.sort()
num_of_elements = len(array)
bubble_sort(array)